#include <stdio.h>
int main()
{
    int i, a, b;
    scanf("%d %d", &a, &b);

    for(i=a; i<=b; i++)
    {

        {
            printf("The positive numbers are %d", i);
        }
        return 0;
    }


    return 0;
}
